services:
  - type: web
    name: invenio-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
